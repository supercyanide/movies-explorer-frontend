const MOVIE_URL = 'https://api.nomoreparties.co';
// const mainApi = 'https://api.movies.supercyanide.nomoredomains.rocks';
const mainApi = 'http://localhost:3000';
export default mainApi;

export {
  MOVIE_URL,
};

export const shortMoviesDuration = 40

const desktopWidth =1280;
const tabletWidth =768; 

const mobileDefaultWidth = 5;
const tabletDefaultWidth = 8;
const desktopDefaultWidth = 12;

const mobileOffset = 1;
const tabletOffset = 2;
const desktopOffset = 3;

export {
  mobileDefaultWidth,
  tabletDefaultWidth,
  desktopDefaultWidth,
  mobileOffset,
  tabletOffset,
  desktopOffset,
  desktopWidth,
  tabletWidth
};